server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configs/

# Example GET /actuator/health response:
# {
#   "status": "UP",
#   "components": {
#     "diskSpace": {
#       "status": "UP"
#     },
#     "ping": {
#       "status": "UP"
#     }
#   }
# }
