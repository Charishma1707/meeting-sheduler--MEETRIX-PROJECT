spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/eventdb
    username: postgres
    password: admin
  kafka:
    bootstrap-servers: localhost:9092

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
