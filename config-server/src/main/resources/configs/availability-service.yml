spring:
  data:
    redis:
      host: localhost
      port: 6379

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

cache:
  ttl: 3600

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
